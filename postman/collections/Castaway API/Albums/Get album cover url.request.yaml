$kind: http-request
url: "{{baseUrl}}/albums/:id/cover"
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: id
    value: <string>
    description: "(Required) "
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 3000
