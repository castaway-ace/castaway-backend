$kind: http-request
url: "{{baseUrl}}/admin/whitelist/:id"
method: DELETE
pathVariables:
  - key: id
    value: <string>
    description: "(Required) "
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 4000
