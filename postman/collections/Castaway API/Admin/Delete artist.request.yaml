$kind: http-request
url: "{{baseUrl}}/admin/artists/:id"
method: DELETE
pathVariables:
  - key: id
    value: <string>
    description: "(Required) "
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 3000
