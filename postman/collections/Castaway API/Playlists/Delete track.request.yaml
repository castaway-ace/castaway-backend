$kind: http-request
url: "{{baseUrl}}/playlists/:id/tracks/:trackId"
method: DELETE
pathVariables:
  - key: id
    value: <string>
    description: "(Required) "
  - key: trackId
    value: <string>
    description: "(Required) "
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 9000
