$kind: http-request
url: "{{baseUrl}}/tracks/:id/stream"
method: GET
headers:
  - key: range
    value: <string>
    description: "(Required) "
pathVariables:
  - key: id
    value: <string>
    description: "(Required) "
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 3000
