$kind: http-request
url: "{{baseUrl}}/interactions/artists/:id"
method: POST
pathVariables:
  - key: id
    value: <string>
    description: "(Required) "
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 3000
